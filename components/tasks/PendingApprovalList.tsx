"use client";

// Parent dashboard: fetches GET /api/tasks?status=PENDING and renders an
// ApproveTaskCard per task. Owns its own loading/error state.
export function PendingApprovalList() {
  // TODO: fetch PENDING tasks, handle loading/error, render ApproveTaskCards.
  return null;
}
