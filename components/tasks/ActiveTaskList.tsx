"use client";

// Kid board: fetches GET /api/tasks?status=CLAIMED and renders a TaskCard per
// task. Owns its own loading/error state.
export function ActiveTaskList() {
  // TODO: fetch CLAIMED tasks, handle loading/error, render TaskCards.
  return null;
}
