"use client";

// shadcn: Input, Label, Button. Fetches GET /api/settings on mount; phone
// number input with a save button -> PATCH /api/settings.
export function SettingsForm() {
  // TODO: load settings, controlled phone input, save via PATCH, inline errors.
  return null;
}
