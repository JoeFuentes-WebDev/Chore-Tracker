"use client";

// Parent dashboard: fetches GET /api/proposals (status = PENDING) and renders
// the parent variant of ProposalCard per proposal. Owns loading/error state.
export function ProposalReviewList() {
  // TODO: fetch pending proposals, handle loading/error, render parent ProposalCards.
  return null;
}
