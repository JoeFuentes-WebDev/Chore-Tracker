// Earnings History — /history (see 6.6)
// Read-only list of all approved tasks. Visible in both kid and parent modes.
//   - Total earnings card (top)
//   - List of completed TaskCards (no action buttons)

export default function HistoryPage() {
  // TODO: fetch GET /api/tasks?status=APPROVED, show total + TaskCard list.
  return <main className="pb-20" />;
}
