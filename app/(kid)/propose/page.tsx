// Propose Chore — /propose (see 6.3)
// Kid submits a new chore idea and sees their pending proposals.
//   - ProposeChoreForm  (name, emoji picker, suggested reward)
//   - ProposalCard list (pending proposals)

export default function ProposePage() {
  // TODO: render ProposeChoreForm and a list of pending ProposalCards.
  return <main className="pb-20" />;
}
