// Settings — /settings (see 6.7), parent only.
// Minimal: parent phone number for SMS notifications.
//   - SettingsForm

export default function SettingsPage() {
  // TODO: render SettingsForm.
  return <main className="pb-20" />;
}
