// Manage Chores — /manage (see 6.5)
// Parent-only full chore library management.
//   - AddChoreForm        (create a chore)
//   - ChoreLibraryList    (grouped recurring vs one-time; parent ChoreCard)

export default function ManagePage() {
  // TODO: render AddChoreForm and ChoreLibraryList.
  return <main className="pb-20" />;
}
